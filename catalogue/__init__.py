"""Catalogue enrichment & the host keepsake (Phase 4+).

Will turn resolved part IDs into the visit deliverable: counts by type/colour, a
BrickLink-based valuation, and a Rebrickable "sets you could build from what you own"
list. Uses Rebrickable + BrickLink (API keys via `.env`). Stub for now.
"""
